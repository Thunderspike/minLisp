(define factorial(n)
	(if (> n 0)   
		(* n (factorial (- n 1)))
		1
	)
)

(define main()
    ( let ( (x 1) (limit 10) )
	( while (< x limit)      # using recursion would be better       
		(seq (write (factorial x))(set x (+ x 1)))
        )
   )
)
